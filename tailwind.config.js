module.exports = {
  darkMode: 'class', // Enables dark mode based on a class (e.g., 'dark')
  theme: {
    extend: {}, // You can extend Tailwind's default theme here, such as adding custom colors, spacing, etc.
  },
  plugins: [], // You can add any plugins here if needed (e.g., for forms, typography, etc.)
}